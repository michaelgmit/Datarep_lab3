import React from 'react';

export class Footer extends React.Component{

    render(){ // these elements will be brought into the app.js class
        return( 
            <div>
                <h1>This is the footer component.</h1>
            </div>
        );

    }
}